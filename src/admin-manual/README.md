# Admin Manual

Placeholder. The admin manual will scaffold here once the editor manual is established. It will cover:

- Installation, configuration, deployment.
- Federation: Pluriverse application, peer admission, handshake, whitelist management.
- Key management: instance identity, log key, PII keys (Pluriverse-host operators only), Pluriverse coordination key, rotation playbooks.
- Backups, snapshots, restore.
- Schema migrations and the schema-mismatch banner.
- Bridges: configuring `TELARIS_BRIDGES`, Mocambos as the first bridge.
- Compromise response: marking a peer compromised, rotating a compromised key, recovering from a coord-key compromise.
- Day-to-day operator runbook (expansions of v10 federation-plan sketches).

Content authored: none yet. Build skipped until `meta.yaml` and at least one `NN-section.md` exist.
